<Window x:Class="MarkRead.App.UI.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarkRead.App.UI.Settings"
        mc:Ignorable="d"
        Title="Settings" 
        Height="700" 
        Width="1000"
        MinHeight="500"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource DefaultWindow}"
        ShowInTaskbar="False"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- Tab Item Style -->
        <Style x:Key="SettingsTabItemStyle" TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThemeButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ThemeAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Background="{DynamicResource ThemeBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource ThemeBackgroundSecondaryBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="24,16">
            <TextBlock Text="Settings"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
        </Border>
        
        <!-- Tab Control -->
        <TabControl Grid.Row="1"
                    x:Name="SettingsTabControl"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="0"
                    ItemContainerStyle="{DynamicResource SettingsTabItemStyle}">
            
            <!-- Viewer Settings Tab -->
            <TabItem Header="Viewer">
                <local:ViewerSettingsView x:Name="ViewerSettingsPanel"/>
            </TabItem>
            
            <!-- Folder Exclusions Tab -->
            <TabItem Header="Folder Exclusions">
                <local:FolderExclusionsView x:Name="FolderExclusionsPanel"/>
            </TabItem>
            
        </TabControl>
        
        <!-- Footer with action buttons -->
        <Border Grid.Row="2"
                Background="{DynamicResource ThemeBackgroundSecondaryBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Reset to Defaults button on left -->
                <Button Grid.Column="0"
                        x:Name="ResetButton"
                        Content="Reset to Defaults"
                        Click="ResetButton_Click"
                        HorizontalAlignment="Left"
                        Padding="16,8"
                        MinWidth="140"
                        Style="{DynamicResource DefaultButton}"/>
                
                <!-- Save and Cancel buttons on right -->
                <Button Grid.Column="1"
                        x:Name="SaveButton"
                        Content="Save"
                        Click="SaveButton_Click"
                        IsDefault="True"
                        Margin="0,0,8,0"
                        Padding="24,8"
                        MinWidth="100"
                        Style="{DynamicResource AccentButton}"/>
                
                <Button Grid.Column="2"
                        x:Name="CancelButton"
                        Content="Cancel"
                        Click="CancelButton_Click"
                        IsCancel="True"
                        Padding="24,8"
                        MinWidth="100"
                        Style="{DynamicResource DefaultButton}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
