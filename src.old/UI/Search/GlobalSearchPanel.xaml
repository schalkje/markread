<UserControl x:Class="MarkRead.App.UI.Search.GlobalSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarkRead.App.UI.Search"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="350"
             x:Name="GlobalSearchPanelControl"
             Visibility="Collapsed">
    
    <UserControl.Resources>
        <!-- Show animation (T074) -->
        <Storyboard x:Key="ShowAnimation">
            <DoubleAnimation Storyboard.TargetName="PanelBorder"
                           Storyboard.TargetProperty="Width"
                           From="0"
                           To="350"
                           Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="PanelBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0"
                           To="1"
                           Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Hide animation (T074) -->
        <Storyboard x:Key="HideAnimation" Completed="OnHideAnimationCompleted">
            <DoubleAnimation Storyboard.TargetName="PanelBorder"
                           Storyboard.TargetProperty="Width"
                           To="0"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="PanelBorder"
                           Storyboard.TargetProperty="Opacity"
                           To="0"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Search input style with icon support (T072) -->
        <Style x:Key="SearchInputWithIcon" TargetType="TextBox">
            <Setter Property="Padding" Value="40,12,12,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <!-- Search icon -->
                                <Path Data="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                                      Fill="{DynamicResource ThemeSecondaryForegroundBrush}"
                                      Width="16"
                                      Height="16"
                                      Stretch="Uniform"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"
                                      Margin="12,0,0,0"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                            Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Scope selector style (T072) -->
        <Style x:Key="ScopeComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Result item style (T072) -->
        <Style x:Key="ResultItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ThemeButtonHoverBrush}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ThemeSelectionBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Result file name style (T072) -->
        <Style x:Key="ResultFileName" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <!-- Result line info style (T072) -->
        <Style x:Key="ResultLineInfo" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeSecondaryForegroundBrush}"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>

        <!-- Result preview style (T072) -->
        <Style x:Key="ResultPreview" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="MaxHeight" Value="60"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="PanelBorder"
            Background="{DynamicResource ThemeBackgroundBrush}"
            BorderBrush="{DynamicResource ThemeBorderBrush}"
            BorderThickness="1,0,0,0"
            Width="350">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with title and close button -->
            <Grid Grid.Row="0" Margin="16,16,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="Search Files"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Foreground="{DynamicResource ThemeForegroundBrush}"
                          VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                       Content="×"
                       FontSize="20"
                       Width="32"
                       Height="32"
                       Background="Transparent"
                       BorderThickness="0"
                       Foreground="{DynamicResource ThemeForegroundBrush}"
                       Cursor="Hand"
                       ToolTip="Close (Esc)"
                       Click="OnCloseClicked">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ThemeButtonHoverBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Search input with icon (T072) -->
            <TextBox x:Name="SearchTextBox"
                    Grid.Row="1"
                    Style="{DynamicResource SearchInputWithIcon}"
                    Margin="16,0,16,12"
                    TextChanged="OnSearchTextChanged"
                    KeyDown="OnSearchKeyDown"/>

            <!-- Scope selector (T072) -->
            <ComboBox x:Name="ScopeSelector"
                     Grid.Row="2"
                     Style="{DynamicResource ScopeComboBox}"
                     Margin="16,0,16,12"
                     SelectedIndex="0"
                     SelectionChanged="OnScopeChanged">
                <ComboBoxItem Content="Current Folder"/>
                <ComboBoxItem Content="Open Files"/>
                <ComboBoxItem Content="Entire Workspace"/>
            </ComboBox>

            <!-- Results list (T072) -->
            <Grid Grid.Row="3">
                <!-- Results -->
                <ListBox x:Name="ResultsList"
                        ItemContainerStyle="{DynamicResource ResultItemStyle}"
                        Background="Transparent"
                        BorderThickness="0"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        MouseDoubleClick="OnResultDoubleClick"
                        Visibility="Collapsed">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding FileName}"
                                          Style="{DynamicResource ResultFileName}"/>
                                <TextBlock Style="{DynamicResource ResultLineInfo}">
                                    <Run Text="Line "/>
                                    <Run Text="{Binding LineNumber}"/>
                                    <Run Text=" • "/>
                                    <Run Text="{Binding MatchCount}"/>
                                    <Run Text=" matches"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Preview}"
                                          Style="{DynamicResource ResultPreview}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Empty state (no results) -->
                <StackPanel x:Name="NoResultsPanel"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed">
                    <Path Data="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                          Fill="{DynamicResource ThemeSecondaryForegroundBrush}"
                          Width="48"
                          Height="48"
                          Stretch="Uniform"
                          Opacity="0.3"
                          Margin="0,0,0,16"/>
                    <TextBlock Text="No results found"
                              FontSize="16"
                              Foreground="{DynamicResource ThemeSecondaryForegroundBrush}"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Try a different search term"
                              FontSize="13"
                              Foreground="{DynamicResource ThemeSecondaryForegroundBrush}"
                              Margin="0,8,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Initial state (search prompt) -->
                <StackPanel x:Name="SearchPromptPanel"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Visible">
                    <Path Data="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                          Fill="{DynamicResource ThemeSecondaryForegroundBrush}"
                          Width="48"
                          Height="48"
                          Stretch="Uniform"
                          Opacity="0.3"
                          Margin="0,0,0,16"/>
                    <TextBlock Text="Search across files"
                              FontSize="16"
                              Foreground="{DynamicResource ThemeSecondaryForegroundBrush}"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Enter a search term to begin"
                              FontSize="13"
                              Foreground="{DynamicResource ThemeSecondaryForegroundBrush}"
                              Margin="0,8,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
