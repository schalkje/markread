<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:MarkRead.Views"
             xmlns:behaviors="clr-namespace:MarkRead.Behaviors"
             x:Class="MarkRead.MainPage">

    <ContentPage.Behaviors>
        <behaviors:KeyboardShortcutBehavior />
    </ContentPage.Behaviors>

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <!-- Navigation Bar -->
            <RowDefinition Height="Auto" />
            <!-- Tab Bar -->
            <RowDefinition Height="Auto" />
            <!-- Main Content with Sidebar -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Navigation Bar -->
        <views:NavigationBar x:Name="NavigationBar" Grid.Row="0" />
        
        <!-- Tab Bar -->
        <Border Grid.Row="1" 
                BackgroundColor="{DynamicResource SurfaceColor}"
                Stroke="{DynamicResource Gray300}"
                StrokeThickness="0,0,0,1">
            <views:TabBar x:Name="TabBar" IsVisible="{Binding Tabs.Count, Converter={StaticResource CountToVisibilityConverter}}" />
        </Border>
        
        <!-- Main Content Area with Sidebar -->
        <Grid x:Name="ContentGrid" Grid.Row="2">
            <Grid.Behaviors>
                <behaviors:EdgeSwipeBehavior x:Name="SidebarEdgeSwipe" />
            </Grid.Behaviors>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="280" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border x:Name="Sidebar" 
                    Grid.Column="0"
                    BackgroundColor="{DynamicResource SurfaceColor}"
                    Stroke="{DynamicResource Gray300}"
                    StrokeThickness="0,0,1,0"
                    IsVisible="True"
                    Padding="0">
                <views:FileTreeView x:Name="FileTree" />
            </Border>

            <!-- Splitter -->
            <Border x:Name="Splitter"
                    Grid.Column="1"
                    WidthRequest="6"
                    BackgroundColor="{DynamicResource Gray200}"
                    HorizontalOptions="Fill">
                <Border.GestureRecognizers>
                    <PanGestureRecognizer PanUpdated="OnSplitterPanUpdated" />
                </Border.GestureRecognizers>
                <BoxView WidthRequest="1" 
                         BackgroundColor="{DynamicResource Gray300}"
                         HorizontalOptions="Center" />
            </Border>

            <!-- Main Content Area -->
            <Grid x:Name="ContentArea" 
                  Grid.Column="2"
                  BackgroundColor="{DynamicResource BackgroundColor}">
                <Grid.Behaviors>
                    <behaviors:SwipeNavigationBehavior x:Name="TabSwipeNav" />
                </Grid.Behaviors>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <!-- Search Bar -->
                <views:SearchBar x:Name="SearchBar" Grid.Row="0" />
                
                <!-- Document Content -->
                <ContentView x:Name="MarkdownViewContainer" 
                             Grid.Row="1"
                             BackgroundColor="{DynamicResource BackgroundColor}" />
            </Grid>
        </Grid>
    </Grid>

</ContentPage>
