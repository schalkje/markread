<UserControl x:Class="MarkRead.UI.Sidebar.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10,5">
            <TextBlock x:Name="RootFolderText" FontWeight="Bold" Text="Folder" TextTrimming="CharacterEllipsis"/>
        </Border>

        <TreeView Grid.Row="1" 
                  x:Name="FileTreeView"
                  BorderThickness="0"
                  Padding="5"
                  MouseDoubleClick="FileTreeView_MouseDoubleClick">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="False"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   x:Name="EmptyStateText"
                   Text="No folder opened"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
