<UserControl x:Class="MarkRead.App.UI.Start.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MarkRead.App.UI.Start"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600">
    <Grid Background="{DynamicResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="340"
                    TextElement.FontSize="16"
                    TextElement.FontFamily="Segoe UI">
            <TextBlock Text="Welcome to MarkRead"
                       FontSize="28"
                       FontWeight="SemiBold"
                       TextAlignment="Center"
                       Margin="0,0,0,24" />
            <TextBlock Text="Open a folder to start exploring your Markdown documentation."
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,0,0,16" />
            <Button Content="Open Folder"
                    HorizontalAlignment="Center"
                    Padding="24,12"
                    Command="{x:Static local:StartCommands.OpenFolder}" />
            <Button Content="Open File"
                    HorizontalAlignment="Center"
                    Padding="24,12"
                    Margin="0,12,0,0"
                    Command="{x:Static local:StartCommands.OpenFile}" />
            <TextBlock Text="You can also drag and drop a folder onto the window."
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,24,0,0"
                       Foreground="Gray" />
        </StackPanel>
    </Grid>
</UserControl>
