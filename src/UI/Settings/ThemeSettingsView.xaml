<Window x:Class="MarkRead.UI.Settings.ThemeSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Theme Settings"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Appearance" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Theme Mode:" Margin="0,0,0,5"/>
        
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <RadioButton x:Name="SystemThemeRadio" Content="System (Auto)" Margin="0,5,0,5" GroupName="Theme" Checked="ThemeRadio_Checked"/>
            <RadioButton x:Name="LightThemeRadio" Content="Light" Margin="0,5,0,5" GroupName="Theme" Checked="ThemeRadio_Checked"/>
            <RadioButton x:Name="DarkThemeRadio" Content="Dark" Margin="0,5,0,5" GroupName="Theme" Checked="ThemeRadio_Checked"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Other Settings:" Margin="0,0,0,5"/>
        
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <CheckBox x:Name="AutoReloadCheckBox" Content="Auto-reload files on change" Margin="0,5,0,5"/>
            <CheckBox x:Name="ShowFileTreeCheckBox" Content="Show file tree sidebar" Margin="0,5,0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
